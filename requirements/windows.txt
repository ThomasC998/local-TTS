# Windows with an NVIDIA GPU. Run: pip install -r requirements/windows.txt
#
# IMPORTANT: torch is NOT listed here, and that is deliberate. The CUDA build
# comes from NVIDIA's own package index, which a plain `pip install torch`
# does not use -- it installs a CPU-only wheel instead, and the server then
# refuses to start with "no CUDA device is visible to PyTorch". Install it
# first, or just run install.ps1, which does this for you:
#
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
#
-r common.txt

# The global keyboard hook. This is Windows' Hammerspoon.
pynput>=1.7

# Reads the INT8 checkpoint, which is what download_model.py fetches by default.
# It is not optional there: importing it is what registers the tensor classes
# torch.load needs, and what makes those weights usable in a linear layer.
# Pinned, because 0.18+ requires torch >= 2.11 and the CUDA wheels installed
# here are 2.9.
torchao==0.17.0
