# Everything both platforms need. The per-platform files install this first.
#
# Lower bounds, not pins: this is an application people install once, and a
# pinned transitive tree goes stale faster than it prevents breakage. The two
# exceptions are in the platform files, where the version really does have to
# match the hardware.

# Server
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9

# Audio: soundfile writes the archive, soxr resamples to the device rate, and
# sounddevice is the streaming player. See audio_out.py for why all three.
numpy>=2.0,<3
soundfile>=0.13
soxr>=0.5
sounddevice>=0.5

# Model plumbing. transformers supplies the text tokenizer, qwen-tts the audio
# codec -- which also needs the native SoX executable, installed separately.
transformers>=4.57,<5
safetensors>=0.8
qwen-tts==0.1.1
huggingface_hub>=0.34

# Configuration, the language-model providers, and memory reporting.
python-dotenv>=1.0
requests>=2.31
google-genai>=1.0
psutil>=5.9
